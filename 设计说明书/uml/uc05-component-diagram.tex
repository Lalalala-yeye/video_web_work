\documentclass[tikz,border=10pt]{standalone}
\usepackage[UTF8]{ctex}
\input{component-style.tex}
\begin{document}
\begin{tikzpicture}
  \diagramtitle{UC05 关注与订阅动态——UML 组件图}
    {展示关注关系维护以及关注作者视频、直播内容聚合所依赖的组件}

  \node[actor] (follower) at (0,3.75) {登录用户\\（关注者）};
  \node[component] (showcase) at (4,5.7) {\textbf{UserShowcaseView}\\\footnotesize 关注／取消关注入口};
  \node[component] (subscribe) at (4,1.8) {\textbf{SubscribeView}\\\footnotesize 订阅动态列表};
  \node[component] (client) at (8.5,3.75) {\textbf{subscription.js}\\\footnotesize 关注状态与动态请求};

  \node[component] (security) at (13,3.75) {\textbf{SecurityConfig + JWT Filter}\\\footnotesize 登录身份校验};
  \node[component] (controller) at (17.5,3.75) {\textbf{SubscriptionController}\\\footnotesize follow、status、feed};
  \node[component] (service) at (22,3.75) {\textbf{SubscriptionService}\\\footnotesize 关系维护与内容聚合};

  \node[component] (mapper) at (26.5,3.75) {\textbf{Mapper 层}\\\footnotesize Subscription / Video / LiveRoom};
  \node[datastore] (mysql) at (31,3.75) {\textbf{MySQL 8}\\\footnotesize subscriptions\\videos / live\_rooms};

  \draw[request] (follower) to[out=35,in=180] (showcase);
  \draw[request] (follower) to[out=-35,in=180] (subscribe);
  \draw[request] (showcase) -- (client);
  \draw[request] (subscribe) -- (client);
  \draw[request] (client) -- node[labeltext,above]{HTTP + JWT} (security);
  \draw[request] (security) -- (controller);
  \draw[dependency] (controller) -- (service);
  \draw[dependency] (service) -- node[labeltext,above]{查询／写入} (mapper);
  \draw[dependency] (mapper) -- (mysql);

  \foreach \n in {showcase,subscribe,client,security,controller,service,mapper}{\componentglyph{\n}}
  \begin{scope}[on background layer]
    \node[group, fill=frontendfill, fit=(showcase)(subscribe)(client)] (fg) {};
    \node[group, fill=backendfill, fit=(security)(controller)(service)] (bg) {};
    \node[group, fill=datafill, fit=(mapper)(mysql)] (dg) {};
  \end{scope}
  \groupcaption{fg}{Vue 3 前端}
  \groupcaption{bg}{Spring Boot 后端}
  \groupcaption{dg}{数据基础设施}
  \node[note, anchor=north west] at (-1.4,-0.4) {\textbf{核心规则：}\quad (follower\_id, target\_id) 组合唯一，避免重复关注；订阅动态仅聚合已关注作者的公开视频和当前直播间，并支持分页。};
\end{tikzpicture}
\end{document}
