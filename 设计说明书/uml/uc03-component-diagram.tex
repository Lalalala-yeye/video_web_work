\documentclass[tikz,border=10pt]{standalone}
\usepackage[UTF8]{ctex}
\input{component-style.tex}
\begin{document}
\begin{tikzpicture}
  \diagramtitle{UC03 视频浏览与播放——UML 组件图}
    {展示公开视频浏览、媒体播放、播放进度保存与历史续播所依赖的组件}

  \node[actor] (viewer) at (0,3.75) {访客／登录用户};
  \node[component] (home) at (4,5.7) {\textbf{HomeView}\\\footnotesize 公热视频列表};
  \node[component] (detail) at (4,3.1) {\textbf{VideoDetailView}\\\footnotesize HTML5 视频播放器};
  \node[component] (profile) at (4,0.5) {\textbf{ProfileView}\\\footnotesize 播放历史与续播};
  \node[component] (client) at (8.5,3.1) {\textbf{video.js + request.js}\\\footnotesize 列表、详情与进度请求};

  \node[component] (security) at (13,3.1) {\textbf{SecurityConfig + JWT Filter}\\\footnotesize 公开读取／登录历史};
  \node[component] (controller) at (17.5,3.1) {\textbf{VideoController}\\\footnotesize list、getone、history};
  \node[component] (service) at (22,3.1) {\textbf{VideoService}\\\footnotesize 可见性与历史规则};

  \node[component] (mapper) at (26.5,4.6) {\textbf{Mapper 层}\\\footnotesize VideoMapper · PlayHistoryMapper};
  \node[datastore] (mysql) at (31,5.2) {\textbf{MySQL 8}\\\footnotesize videos / play\_history};
  \node[component] (uploads) at (31,1.6) {\textbf{uploads/}\\\footnotesize 视频与封面静态资源};

  \draw[request] (viewer) to[out=40,in=180] (home);
  \draw[request] (viewer) -- node[labeltext,above]{浏览／播放} (detail);
  \draw[request] (viewer) to[out=-40,in=180] (profile);
  \draw[request] (home) -- (client);
  \draw[request] (detail) -- (client);
  \draw[request] (profile) -- (client);
  \draw[request] (client) -- node[labeltext,above]{HTTP} (security);
  \draw[request] (security) -- (controller);
  \draw[dependency] (controller) -- (service);
  \draw[dependency] (service) -- (mapper);
  \draw[dependency] (mapper) -- (mysql);
  \draw[request] (detail.east) to[out=-25,in=180] node[labeltext,below]{GET /uploads/**} (uploads.west);

  \foreach \n in {home,detail,profile,client,security,controller,service,mapper,uploads}{\componentglyph{\n}}
  \begin{scope}[on background layer]
    \node[group, fill=frontendfill, fit=(home)(detail)(profile)(client)] (fg) {};
    \node[group, fill=backendfill, fit=(security)(controller)(service)] (bg) {};
    \node[group, fill=datafill, fit=(mapper)(mysql)(uploads)] (dg) {};
  \end{scope}
  \groupcaption{fg}{Vue 3 前端}
  \groupcaption{bg}{Spring Boot 后端}
  \groupcaption{dg}{数据与文件基础设施}
  \node[note, anchor=north west] at (-1.4,-1.0) {\textbf{核心规则：}\quad 匿名用户仅访问已发布视频；登录用户播放时写入或更新 play\_history，并在再次进入时读取最后进度完成续播。};
\end{tikzpicture}
\end{document}
