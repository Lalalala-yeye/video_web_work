\documentclass[tikz,border=10pt]{standalone}
\usepackage[UTF8]{ctex}
\input{component-style.tex}
\begin{document}
\begin{tikzpicture}
  \diagramtitle{UC07 关键词搜索——UML 组件图}
    {展示关键词校验、视频／直播／用户联合检索及结果跳转所依赖的组件}

  \node[actor] (user) at (0,3.75) {访客／登录用户};
  \node[component] (nav) at (4,5.7) {\textbf{TopNavigation}\\\footnotesize 关键词输入与提交};
  \node[component] (searchView) at (4,1.8) {\textbf{SearchView}\\\footnotesize 分类结果与跳转};
  \node[component] (client) at (8.5,3.75) {\textbf{search.js}\\\footnotesize 搜索参数与统一响应};

  \node[component] (security) at (13,3.75) {\textbf{SecurityConfig}\\\footnotesize /search 允许匿名访问};
  \node[component] (controller) at (17.5,3.75) {\textbf{SearchController}\\\footnotesize keyword 与各类 limit};
  \node[component] (service) at (22,3.75) {\textbf{SearchService}\\\footnotesize 模糊匹配与结果组装};

  \node[component] (mapper) at (26.5,3.75) {\textbf{Mapper 层}\\\footnotesize Video / LiveRoom / User};
  \node[datastore] (mysql) at (31,3.75) {\textbf{MySQL 8}\\\footnotesize videos / live\_rooms / users};

  \draw[request] (user) to[out=35,in=180] (nav);
  \draw[request] (user) to[out=-35,in=180] (searchView);
  \draw[request] (nav) -- (client);
  \draw[request] (searchView) -- (client);
  \draw[request] (client) -- node[labeltext,above]{GET /search} (security);
  \draw[request] (security) -- (controller);
  \draw[dependency] (controller) -- (service);
  \draw[dependency] (service) -- node[labeltext,above]{查询并组装} (mapper);
  \draw[dependency] (mapper) -- (mysql);

  \foreach \n in {nav,searchView,client,security,controller,service,mapper}{\componentglyph{\n}}
  \begin{scope}[on background layer]
    \node[group, fill=frontendfill, fit=(nav)(searchView)(client)] (fg) {};
    \node[group, fill=backendfill, fit=(security)(controller)(service)] (bg) {};
    \node[group, fill=datafill, fit=(mapper)(mysql)] (dg) {};
  \end{scope}
  \groupcaption{fg}{Vue 3 前端}
  \groupcaption{bg}{Spring Boot 后端}
  \groupcaption{dg}{数据基础设施}
  \node[note, anchor=north west] at (-1.4,-0.4) {\textbf{核心规则：}\quad 关键词去除首尾空白后不能为空；视频结果只返回可公开访问内容，并分别限制视频、直播间和用户的返回数量。};
\end{tikzpicture}
\end{document}
