# 特征用例：UC-12 关键词搜索

选它作为代表：对外接口拆分前后都是 `GET /search`，最能把「需求 → 设计 → 代码 → 测试」和微服务边界放在同一条链上。编号全程不变：`UC-12` = `REQ-12` = `OP-028` = 模块 `M6`。

四层对照：

| 需求条款 | 设计 | 代码 | 测试 |
|---|---|---|---|
| 顶栏输入关键词发起搜索 | 概要设计图 2-12：SearchView / 顶栏 → search.js → SearchController | `TopNavigation.onSearch` 空词直接 return；`search.js` 打 `/search` | E2E `02-browse.js` 必须走顶栏，禁止 `driver.get('/search')` |
| 一次返回视频、用户、直播 | 详细设计 M6：输出 `{videos, users, liveRooms}`；搜索无独立表，网关并行聚合 | 网关 `SearchServiceImpl` 三路 `CompletableFuture` | S000 用户非空；E2E 分别搜到视频卡 / 直播卡 / 用户并跳转 |
| 只搜已发布视频 | 模型基线 oracle：视频结果仅 status=1 | 视频服务 `searchPublished`，内部路径 `/internal/search/videos` | S000 命中已审内容；待审稿不进公开搜索 |
| 空词不搜 | M6：空关键词三类均为 `[]` | 前端 trim 后为空不跳转；后端 `hasText` 为假则不调下游 | 单测 `search_whenKeywordBlank_returnsEmptyListsWithoutCallingDownstream` |
| 无匹配给空页 | 扩展流 3a | 三路都返回空列表，`code` 仍 200 | S001、S002；E2E「没有找到相关视频/直播/用户」 |
| 95% 在 3 秒内返回；查询超时 | 单路超时 3 秒；失败降级而不是整页报错 | `orTimeout(3, SECONDS)`，失败写入 `notices` | 单测下游 502 仍 200 且黄条；`fault-demo.ps1` 停 video 后现场复现 |

---

## 1. 具体需求（SRS UC-12 / REQ-12）

参与者：访客或登录用户。触发：顶部导航搜索框提交。关联操作：OP-028 `GET /search`。

**基本事件流**

1. 用户输入关键词并发起搜索。
2. 系统对**已发布视频标题**、**用户昵称**、**直播间标题**做模糊匹配。
3. 返回视频、用户、直播三类结果。
4. 用户点击结果，分别进入视频详情、用户主页或直播间。

**扩展 / 约束**

- 关键词为空：阻止搜索。
- 无匹配：空结果页，不报错。
- 查询超时：不能把整站搜挂掉（SRS 原文写「提示重试」；实现改为该路空列表 + `notices`，其它路照出）。
- 性能：95% 搜索 3 秒内返回。
- 视频范围默认仅已发布。

后置条件：按已发布视频标题能搜到该视频并跳转详情。

---

## 2. 具体设计

**概要设计**：组件顺序图（图 2-12）——顶栏 / `SearchView` → `search.js` → `SearchController` → `SearchService`。公开接口允许匿名。

**详细设计 M6**（图 2-20）：

- 输入：`keyword`，以及 `videoLimit` / `liveLimit` / `userLimit`
- 处理：分别检索已发布视频、直播间、用户；空词或无命中时三类均为 `[]`
- 算法：标题 / 昵称 `LIKE` 后截断条数
- 输出：`{ videos, users, liveRooms }`

**微服务设计（拆分后，路径不改）**

搜索没有自己的表，不单独建服务。网关本进程处理 `/search`（`GatewayProxyFilter` 不转发这条），并行调用：

- 用户服务 `GET /internal/search/users`
- 视频服务 `GET /internal/search/videos`（只查 `videos`，且已发布）
- 直播服务 `GET /internal/search/lives`（只查 `live_rooms`）

浏览器打不到 `/internal/**`。单路 3 秒放弃，整体仍 `code=200`。

---

## 3. 对应代码（按需求条款摘）

**需求 1：顶栏发起；空词不搜**

```javascript
// web/src/components/layout/TopNavigation.vue
function onSearch() {
  const q = searchQuery.value.trim()
  if (!q) return
  router.push({ path: '/search', query: { keyword: q } })
}
```

```javascript
// web/src/api/search.js  —— 对应 OP-028，路径与单体相同
export function search(keyword, config = {}, videoLimit = 12, liveLimit = 12, userLimit = 12) {
  return get('/search', { keyword, videoLimit, liveLimit, userLimit }, config)
}
```

**需求 2～3 + 设计 M6：对外仍是一个 Search，对内三路聚合**

```java
// services/doinb-gateway/.../SearchController.java  —— OP-028
@GetMapping("/search")
public CustomResponse search(@RequestParam("keyword") String keyword,
                             @RequestParam(value = "videoLimit", defaultValue = "10") long videoLimit,
                             @RequestParam(value = "liveLimit", defaultValue = "10") long liveLimit,
                             @RequestParam(value = "userLimit", defaultValue = "10") long userLimit) {
    return CustomResponse.ok(searchService.search(keyword, videoLimit, liveLimit, userLimit));
}
```

```java
// SearchServiceImpl.search —— 空词不调下游；三路并行，对应「一次返回三类」
if (!StringUtils.hasText(keyword)) {
    return result;
}
CompletableFuture<Bucket> videos = fetch(s.getVideo(), InternalPaths.SEARCH_VIDEOS, kw, clamp(videoLimit));
CompletableFuture<Bucket> lives  = fetch(s.getLive(),  InternalPaths.SEARCH_LIVES,  kw, clamp(liveLimit));
CompletableFuture<Bucket> users  = fetch(s.getUser(),  InternalPaths.SEARCH_USERS,  kw, clamp(userLimit));
```

**需求「只搜已发布」**

```java
// doinb-video InternalVideoController —— 内部接口，网关才调
@GetMapping(InternalPaths.SEARCH_VIDEOS)
public CustomResponse search(...) {
    return CustomResponse.ok(videoService.searchPublished(keyword, limit));
}
```

**需求「3 秒」+ 扩展流超时：单路超时降级，不是整页失败**

```java
static final long DOWNSTREAM_TIMEOUT_SECONDS = 3;

return CompletableFuture.supplyAsync(() -> call(...), executor)
        .orTimeout(DOWNSTREAM_TIMEOUT_SECONDS, TimeUnit.SECONDS)
        .exceptionally(ex -> Bucket.down());

if (videoBucket.failed) {
    result.getNotices().add("视频服务超时或不可用，已跳过视频结果（其它分类不受影响）");
}
```

页面用 `el-alert` 展示 `results.notices`，对应「失败要提示」，同时用户 / 直播 tab 仍可有数据。

---

## 4. 对应测试（含实测数据）

层次：单元对着 Search 业务规则；系统测试对着 OP-028 打网关；E2E 对着顶栏和跳转；故障脚本对着超时降级。

### 4.1 单元（网关 `SearchServiceImplTest`，不启 MySQL）

| 测试方法 | 对应需求 | 输入 | 断言 |
|---|---|---|---|
| `search_whenKeywordBlank_...` | 空词不搜 | keyword=`"  "` | 三类空；`ServiceClient.get` **一次都不调** |
| `search_whenNoMatch_...` | 扩展 3a 空页 | `xyznotexist123` | 三类 `[]` |
| `search_whenDownstreamFails_...` | 超时/下游失败隔离 | keyword=`测试`；直播返回 502，视频/用户 200 | videos=1（id=12 测试视频）；users=1（昵称测试昵称）；liveRooms=`[]`；`notices` 含「直播」 |
| `search_whenVideoDown_...` | 故障实验同款 | keyword=`演示`；视频 502 | videos 空；users=1（演示观众）；`notices` 含「视频」、不含「用户」 |

### 4.2 系统测试（《测试报告》S000～S002，打 `http://127.0.0.1:8081`，3/3 通过）

对应 OP-028。Header 均无 Token（公开搜索）。

**S000 搜索成功**（基本流第 2～3 步：能按关键词命中）

- 前置：U080 已把昵称改为「测试昵称」
- 请求：`GET /search?keyword=测试昵称&videoLimit=10&liveLimit=10&userLimit=10`
- 预期：`code=200`，`users` 非空
- 实际：

```json
{"code":200,"message":"OK","data":{"liveRooms":[],"users":[{"id":45,"username":"rpt_a_1787976577510","nickname":"测试昵称","avatar":"/uploads/avatars/2c72dd6e-79dc-475c-a8ec-a2271631f429.png","role":1,"bio":null},{"id":10,"username":"user_a","nickname":"测试昵称","role":1}],"videos":[]}}
```

（命中用户侧模糊匹配；该次库里标题不含「测试昵称」，故 `videos` 为空，符合「只返回匹配项」。）

**S001 无结果**（扩展 3a）

- 请求：`GET /search?keyword=xyznotexist123&videoLimit=10&liveLimit=10&userLimit=10`
- 实际：`{"code":200,"message":"OK","data":{"liveRooms":[],"users":[],"videos":[]}}`

**S002 数字无结果**（避免「纯数字误当成 id」）

- 请求：`keyword=11111xyznotexist`
- 实际：三类仍为 `[]`，`code=200`

### 4.3 验收 E2E（`web/e2e/02-browse.js`，对应基本流第 1 步和第 4 步）

现场上传并审核通过后，用**顶栏**搜索：

- 按视频标题 → 出现 video-card，截图 `02-3-search-video`
- 按直播间标题 → 直播 tab 出现 live-card，并可点进 `/live/{id}`
- 按昵称 → 用户 tab 出现并点进 `/user/`
- 垃圾关键词 → 三个 tab 均为「没有找到相关…」

### 4.4 故障处理（对应扩展流「查询超时」）

`.\deploy\k8s\fault-demo.ps1 -Target k8s`：停掉 video 后再搜。预期与单测 `search_whenVideoDown` 同一条规则——整体 200、`videos=[]`、`notices` 含视频不可用、gateway/user/live/interact/message 的 `/health` 仍 200。

---

**收束一句**：需求规定一次搜三类、空词/空结果/超时要可预期；设计把 M6 放到网关聚合、各服务只查本表；代码里空词短路、三路并行、3 秒降级、`searchPublished` 限制已发布；S000～S002、四条单测、E2E 顶栏和故障脚本分别钉死这几条，没有另起一套编号。


# 微服务划分

前端不拆。后端按**功能域 + 谁拥有哪张表**拆成 5 个业务进程，经网关对外；搜索没有自己的表，不单独建服务。课设期可共用一台 MySQL，逻辑上分归属，禁止跨服务 JOIN。

---

## 1. 模型到代码

领域概念类（SRS 图 3-2）一一落到「本服务的 Entity + Mapper + 表」，其它服务只存对方业务 ID，要数据走内部 HTTP。

例：概念类 `User` → `doinb-user` 的 `User.java` / `UserMapper` / 表 `users`。视频表里的 `author_id` 只是整数，拼昵称时调 `GET /internal/users?ids=`，不 JOIN `users`。`Comment.targetId` 同理，发评论前调视频或直播的 `/internal/.../{id}`，不查 `videos` / `live_rooms`。

调用链：`Vue` → `web/src/api/*.js` → 网关 8081 验 JWT、注入 `X-User-Id` → 下游 Controller → Service → 本表。下游不再解析 JWT。

---

## 2. 划分依据

按「一块业务只写自己的表」切，不按 15 个用例切成 15 个服务；UC-15 审核仍归视频，因为通过/驳回/举报复审都写 `videos` / `video_reports`。

例：UC-10 评论和 UC-11 赞踩都落在互动服务——它们共用 `comments`、`video_reactions`，分开反而要跨库。直播弹幕也走 `CommentController`（`targetType=2`），房间是否开播去问直播服务，不把弹幕表拆到 live。

---

## 3. 五个业务服务 + 网关

**用户服务（8082）**  
管账号和资料，只写 `users`，头像文件归它。对外 `/user`、`/admin/account`。  
例：登录在本服务签发 JWT；别人要昵称只提供 `GET /internal/users/{id}`，密码字段不下发。

**视频服务（8083）**  
管点播、上传、举报、审核（UC-04～07、UC-15），只写 `videos`、`play_history`、`video_reports`，成片和封面归它。对外 `/video`、`/admin/video`（`/video/reaction` 除外）。  
例：审核通过只改本表 `status`，通知作者则 `POST` 消息服务 `/internal/notifications`（type=4）。

**直播服务（8084）**  
管房间元数据和开停播（UC-08），只写 `live_rooms`；画面仍走 SRS。对外 `/live`。  
例：表里存 `stream_key`、`is_live`；互动发弹幕前调 `GET /internal/lives/{id}` 看是否在播，Java 不经手媒体流。

**互动服务（8085）**  
管评论、赞踩、关注（UC-09～11），只写 `comments`、`*_reactions`、`subscriptions`。对外 `/comment`、`/subscription`、`/video/reaction`。本期不对他人提供内部接口。  
例：`CommentServiceImpl` 在 `targetType=1` 时调视频 `/internal/videos/{id}`，目标不存在返回 404「视频不存在」，不自己 SELECT videos。

**消息服务（8086）**  
管通知和私信（UC-13～14），只写 `notifications`、`dm_rooms`、`dm_messages`。对外 `/notification`、`/message`。  
例：点赞方调 `POST /internal/notifications`；私信通知在本进程写会话时一起写，不再 HTTP 调自己。

**网关（8081）**  
验 JWT、按路径转发、拦截 `/internal`；自己做 UC-12 搜索聚合，无业务表。  
例：`RouteTable` 把 `/video/reaction` 指到互动、把 `/video` 指到视频；`/search` 不转发，本进程并行打三个 `/internal/search/*`。

---

## 4. 调用失败怎么处理

同步查询失败：当「目标不存在」，不写脏数据。  
例：`ServiceClient` 读超时 8 秒，捕获后 `code=502`；互动拿到非 200 就当视频不存在，评论/点赞返回 404。

旁路通知失败：主操作已经成功的，通知尽力而为、不回滚。  
例：`MessageNotifier` 审核通过或点赞后 `try/catch` 调消息服务，挂了不影响 `videos.status` 和赞踩记录。

聚合查询失败：单路降级，整次请求仍 200。  
例：搜索单路 3 秒 `orTimeout`，视频挂了 `videos=[]` + `notices`「视频服务超时或不可用」，用户/直播照出；`fault-demo.ps1` 即演示这一条。网关对其余路径 30 秒超时（上传 10 分钟），浏览器打 `/internal` 直接 403。

---

## extra. 为什么搜索（以及健康检查）放在网关——怕老师问

**放进网关的只有 UC-12 搜索，外加 `/health` `/ready` `/version`。** 其它 14 个用例都在五个业务服务里，网关只转发。

**搜索为什么不单独建第六个业务服务？**  
划分原则是「有表才有服务」。搜索不拥有任何表，只是把用户 / 视频 / 直播三路结果拼成原来的 `{videos, users, liveRooms}`。再建一个 `doinb-search` 等于多一个没有数据的进程，请求还要多一跳。

**为什么不塞进视频服务（或用户服务）？**  
若视频服务做 `/search`，它就必须调用户和直播，视频会变成「什么都聚合」的中心；用户挂了或直播挂了，点播主路径也容易被拖死。网关本来就站在三服务前面，并行扇出、单路 3 秒降级，是它该干的编排，不是业务写库。

**和订阅动态（UC-09）的差别？**  
Feed 也跨视频和直播，但关注关系在 `subscriptions` 表里，所以聚合放在**互动服务**（表的主人去调别人）。搜索没有这样的「主表」，所以放网关。

**网关自己还留了探活接口，不是用例。**  
`/health` `/ready` `/version` 给 K8s 探针和答辩看版本号用，不进 RTM 的 15 个 UC；业务 `/health` 各服务自己也有一份。

# 测试与运行结果（关键数据）

课设分层：单元对着 Service 规则；拆服务后 Newman 打网关算跨进程集成；Selenium 对着 GUI。CI 单测或 Newman 红则不打镜像；E2E 失败只出截图、不挡 CD。

## 1. 单元测试

单体 `backend`：`mvn test` **Tests run: 260，失败 0，跳过 1**。有效 **259 = 对象级 179 + MockMvc 80**。

| 层次 | 条数 | 结果 |
|---|---:|---|
| `*ServiceImplTest`（不启 Spring/MySQL） | 179 | 全过 |
| `*ApiTest` MockMvc（不启 MySQL） | 80 | 全过 |

微服务 CI job `services-test`：五服务 + 网关 JUnit（不启 MySQL），约 **171** 条 `@Test`，失败则流水线停。

## 2. 集成测试（系统测试）

不单独做「组件级集成套件」。拆分后用 Postman/Newman 打 **网关 8081 + 五服务 + MySQL**，覆盖 OP-001～OP-044。

| 口径 | 条数 | 结果 |
|---|---:|---|
| 《测试报告》全量 | **59 / 59** | 通过率 100% |
| 按模块 | H1 + U13 + V12 + C4 + R3 + F4 + S3 + L7 + N3 + M3 + A6 | 即上面 59 |
| CI | `doinb.full.postman_collection.json` 全量公开接口 | 失败则不打镜像 |

特征用例对应：**S000 / S001 / S002** 三条，实际输出见上一节。

## 3. 端到端测试

Selenium 脚本 `web/e2e/01`～`05`，覆盖 **UC-01～UC-15** 主路径。

| 口径 | 数据 |
|---|---|
| 报告检查点 | **54 / 54** 通过 |
| 入口 | 前端 8787，API 经网关 8081 |
| CI | `e2e:ci`，`continue-on-error`；失败上传 `e2e/artifacts/` |

## 4. CI/CD

Push → GitHub Actions **Test gate**：

1. 微服务单测（约 171）  
2. Newman 全量（门禁）  
3. Selenium（不挡门禁）  
4. 打 **7 个镜像**（gateway/user/video/live/interact/message/web），tag = git **7 位 SHA**，禁用 `latest`  
5. kind 部署并探活 `/health` `/ready` `/version`

本机：`scripts/k8s-up.ps1` 首次部署；`scripts/local-cd.ps1 -Watch` 等 CI 出镜像后滚动更新。浏览器 http://localhost:8787 左上角版本号变成新 SHA，即一次 CD。

## 5. 自动扩缩容

HPA 只挂压测会打到的 **gateway / video / user**；MySQL、live、interact、message、web 不挂。

| 项 | 值 |
|---|---|
| 副本 | **1～4** |
| 指标 | CPU **50%**（相对 request） |
| 扩容 | 立即，最多翻倍 |
| 缩容 | 先稳 **120s**，再每分钟最多减 20% |
| 演示 | `hpa-demo.ps1`，登录 **50 VU / 约 90s**，看 `kubectl get hpa -w` 副本增加 |

对比压测时必须关掉 HPA，避免副本变化污染数字。

## 6. 故障处理

脚本：`deploy/k8s/fault-demo.ps1 -Target k8s`。主动把 video 副本打到 0。

| 项 | 结果 |
|---|---|
| `GET /search` | 仍 **code=200** |
| `videos` | `[]` |
| `notices` | 「视频服务超时或不可用…」 |
| 其它服务 `/health` | gateway / user / live / interact / message **仍 200** |
| video `/health` | **DOWN** |
| `GET /video/list` | **502**（证明视频确实挂了） |

结论：隔离，不是整站挂掉。对应 UC-12 扩展流「查询超时」和网关 3 秒降级。

## 7. 单体 vs 微服务性能

同一台机（i9-14900HX / 16GB）、同一脚本，**50 VU / 30s / 3 轮**，预热 10s 丢弃，HPA 关闭。记录 `bench/TEMPLATE.md`（2026-09-02）。两边错误率均为 **0**。

| 接口 | 单体吞吐 | 微服务吞吐 | 单体均时 | 微服务均时 | 内存 |
|---|---:|---:|---:|---:|---|
| 登录 | **32.4** req/s | **33.0** req/s | 1.55 s | 1.54 s | 0.85 / **1.88** GiB |
| 视频列表 | **645** req/s | **116** req/s | 118 ms | 443 ms | 1.05 / **2.00** GiB |
| 搜索 | **474** req/s | **83** req/s | 162 ms | 650 ms | 1.05 / **2.06** GiB |

**怎么讲三句**：登录被 BCrypt 卡住，拆不拆几乎一样。列表和搜索单体更快（约 5～6 倍吞吐），因为多一跳网关 + 六个 JVM，单机对比会吃亏。换来的是按表拆库、按服务扩容、搜索一路挂了还能降级——课设要的是能对比、能解释，不是微服务在单机上更快。