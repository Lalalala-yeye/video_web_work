### v0.7

### 问题：

- [ ] 1.前述的两个 AI 融合功能（**AI 审核** + **以图搜视频**）想要实现应该怎么实现？

### 两个功能分别是什么

| 功能 | 现状 | 目标 |
|------|------|------|
| **AI 审核** | 上传后 `status=0`，纯人工在 `/admin/pending` 审 | 机器先扫一遍，高风险自动拦、低风险可自动过，管理员只看疑难件 |
| **以图搜视频** | `/search` 仅标题关键词 `LIKE` | 用户上传一张图，找画面相似或内容相关的已发布视频 |

二者可**独立分支**开发；建议先做 **AI 审核**（与现有审核流衔接紧、演示价值高）。

---

### 推荐总体架构（贴合当前 doinb 技术栈）

```
上传/编辑视频 (VideoServiceImpl.upload/update)
        │
        ├─► [AI 审核] @Async 投递任务 ──► 云内容安全 API / 本地模型
        │         │                              │
        │         └─► video_audit_logs 留痕       └─► 改 status + 通知作者
        │
        └─► [以图搜] 转码/入库后抽帧 ──► Embedding 服务 ──► 向量库
                                              │
搜索页 /search ──► POST /search/by-image──► 向量检索 ──► 返回 VideoDTO 列表
```

**原则**：Java 后端只做编排（触发、落库、鉴权、通知）；**不算力密集型推理**——课程项目优先接**云 API**，本地只跑 ffmpeg 抽帧。

---

### 功能一：AI 审核 — 分阶段实现

#### 阶段 A（最小可演示，约 1～2 人日）

1. **触发点**：在 `VideoServiceImpl.upload` / `updateVideo` 里，当 `status` 变为 `PENDING(0)` 后，调用 `AiAuditService.submit(videoId)`（`@Async` 即可，不必上 MQ）。
2. **审核输入**（由易到难）：
   - 标题 + 简介文本 → 云「文本审核」
   - 封面图 → 云「图片审核」
   - （可选）ffmpeg 每 30s 截 1 帧，最多 N 张 → 云「图片/视频审核」
3. **云厂商**（二选一，有免费额度）：
   - [阿里云内容安全 Green](https://help.aliyun.com/product/28415.html)
   - [腾讯云图片/文本内容安全](https://cloud.tencent.com/document/product/1125)
4. **策略**（`application-local.yml` 可配置）：
   - `block` → `status=3`（仅自己可见）+ 通知作者「未通过机器审核」
   - `review` → 保持 `status=0`，管理后台列表加标签「AI 待人工」
   - `pass` → 可配置：仍人工终审 **或** 直接 `status=1` 并走现有 `notifyVideoApproved`（actor 仍显示 doinb）
5. **留痕表**（执行一次）：

```sql
CREATE TABLE IF NOT EXISTS video_audit_logs (
  id INT PRIMARY KEY AUTO_INCREMENT,
  video_id INT NOT NULL,
  source VARCHAR(32) NOT NULL COMMENT 'text/cover/frame',
  result VARCHAR(16) NOT NULL COMMENT 'pass/review/block',
  detail TEXT,
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (video_id) REFERENCES videos(id)
);
```

6. **后端新增**（建议包名 `service/ai/`）：
   - `AiAuditService` / `AiAuditServiceImpl`
   - `AliyunGreenClient` 或 `TencentCmsClient`（读环境变量 `AI_ACCESS_KEY`，**勿提交仓库**）
   - `GET /admin/video/audit-logs?videoId=` 供管理后台查看 AI 结论
7. **前端**：`AdminPendingView` 表格加一列「AI 结果」；详情抽屉展示 `video_audit_logs`。

#### 阶段 B（加分项）

- 管理员后台「AI 审核配置」页：阈值、是否自动通过低风险
- 举报复审也跑一遍 AI，辅助决策
- 驳回/通过时对比 AI 日志与人工结论，写设计说明书「人机协同」

#### 与现有代码的挂钩点

| 文件 | 改动 |
|------|------|
| `VideoServiceImpl.java` | 上传/更新待审后 `aiAuditService.submit(id)` |
| `AdminVideoServiceImpl.java` | 可选：列表 DTO 带最近一条 AI 结果 |
| `NotificationServiceImpl.java` | 新增 `TYPE_VIDEO_AI_REJECT` 等，actor 仍用 `doinb` |
| `web/src/views/admin/*` | 展示 AI 标签 |

---

### 功能二：以图搜视频 — 分阶段实现

#### 阶段 A（最小可演示：图 → 文 → 现有搜索）

不建向量库，先验证产品流程：

1. 用户在前端 `/search` 增加 Tab「以图搜索」，上传图片。
2. 后端 `POST /search/by-image`（`multipart file` + `topK`）：
   - 调用**多模态大模型 API**（通义千问-VL / 智谱 GLM-4V / 腾讯混元等）生成简短画面描述，例如「一个人在打篮球」
   - 用描述字符串调用现有 `SearchService.search(keyword)` 搜标题
3. 返回结构与现有搜索一致 `{ videos, liveRooms, users }`（live/user 可空）。
4. **优点**：改动小、无需 Milvus；**缺点**：不是真「视觉相似」，只是语义近似。

#### 阶段 B（课程项目完整方案：向量检索）

1. **入库流水线**（视频发布或审核通过后异步）：
   - ffmpeg：`uploads/videos/xxx.mp4` → 每 30s 一帧 → `uploads/frames/{videoId}/`
   - Python 小服务或 Java 调 ONNX：**CLIP** / **中文 CLIP** 对每帧算 embedding（512/768 维）
   - 写入向量库：**Milvus**（Docker 一条命令）或 **MySQL + pgvector**（若愿换库）
   - 表 `video_frame_vectors(video_id, frame_index, vector_blob 或 外键 milvus_id)`
2. **查询**：
   - 用户上传 query 图 → 同模型算 query 向量
   - ANN  topK 帧 → 按 `video_id` 聚合（命中帧数、平均距离）
   - 只返回 `status=1` 的视频，复用 `VideoDTO`
3. **接口**：

| 接口 | 说明 |
|------|------|
| `POST /search/by-image` | multipart 图片，返回相似视频列表 |
| `GET /admin/ai/index-status?videoId=` | 可选，索引进度 |

4. **前端**：结果用现有 `VideoCard` 展示；可加「相似度」百分比（由距离换算）。

#### 阶段 C（可选）

- ASR 字幕入库 + 文本向量，支持「用一句话找片段」
- 搜索结果跳转到视频并带 `?t=秒数` 定位到命中帧

#### 依赖与部署提示

| 组件 | 用途 |
|------|------|
| ffmpeg | 抽帧（本机或服务器安装，Java `ProcessBuilder` 调用） |
| Milvus / Qdrant | 向量存储（Docker） |
| Python FastAPI 微服务 | 跑 CLIP 模型（Java HTTP 调用），避免 JVM 绑大模型 |
| 云 API Key | 阶段 A 只需多模态/内容安全 Key |

---

### 建议实施顺序与分工

| 顺序 | 任务 | 负责人建议 |
|------|------|------------|
| 1 | 建 `video_audit_logs` + `@Async` 骨架 + 配置项 | 后端 |
| 2 | 接云文本/图片审核，打通上传→AI→改状态→通知 | 后端 |
| 3 | 管理后台展示 AI 结论 | 前端 |
| 4 | `/search` 以图 Tab + `POST /search/by-image`（阶段 A 图生文） | 前后端各半 |
| 5 | ffmpeg 抽帧 + 向量服务 + Milvus（阶段 B） | 后端 + 会 Python 的同学 |

---

### 配置示例（`application-local.yml`，勿提交）

```yaml
ai:
  provider: aliyun   # aliyun | tencent
  access-key-id: "你的Key"
  access-key-secret: "你的Secret"
  audit:
    auto-pass-low-risk: false   # true 则 AI pass 直接发布
    auto-block-high-risk: true
  image-search:
    mode: caption   # caption（阶段A）| vector（阶段B）
    embedding-service-url: http://127.0.0.1:5000/embed
```

---

### 风险与课程答辩说明

- **费用**：云 API 按次计费，演示用短视频 + 少抽帧；Key 放环境变量。
- **延时**：审核/索引进度异步，前端创作中心可显示「AI 审核中」。
- **合规**：设计说明书写清「AI 辅助、人工终审」，与现有管理员 UI 一致。
- **做不到向量库时**：阶段 A 的「图生文 + 关键词搜索」仍可答辩为「以图搜视频原型」。

### v0.6

### 问题：

- [x] 1.直播功能完善
  - 弹幕仅显示**本场直播**（自开播起），进入不会看到上一场/更早的弹幕
  - 每天 **0:05** 自动清空全部直播弹幕
  - 公开列表**仅展示正在直播**中的房间；未开播/已停播对观众不可见
  - 停播 **2 小时**后自动删除房间记录；创建后 **24 小时**从未开播也会清理
  - 真实拉流画面**尚未接入**（见下方「直播实现方案」）

- [x] 2.多账号同时登录
  - 活跃账号改为 **按浏览器标签页独立**（`sessionStorage`）
  - 导航「新标签页登录其他账号」→ 新标签页登录不影响原标签页
  - 同一标签页内仍可用「切换账号」

- [x] 3.公网部署说明（见下方「部署与存储方案」）

### 直播实现方案（后续推进）

当前仅有**房间状态 + 弹幕**业务，没有真正的音视频流。要看到直播画面，需要补齐：

| 环节 | 说明 |
|------|------|
| **推流** | 主播用 OBS / ffmpeg 向 RTMP 地址推流，如 `rtmp://你的域名/live/{streamKey}` |
| **流媒体服务** | 部署 **SRS**、**nginx-rtmp** 或云直播（阿里云直播、腾讯云 CSS）做转码、分发 |
| **拉流** | 观众播放 **HLS**（`.m3u8`）或 **FLV**；前端可用 `hls.js` + `<video>` |
| **后端** | 开播时向流媒体服务注册流；停播时断开；`playUrl` 指向 CDN/流媒体输出的 m3u8 |
| **鉴权** | 推流/拉流 URL 带 token 或限时签名，防止盗链 |

推荐最小路径：**云服务器 Docker 跑 SRS** → OBS 推 RTMP → 观众播 HLS。

### 部署与存储方案

**GitHub 能挂什么？**

| 方式 | 能挂 | 不能挂 |
|------|------|--------|
| **GitHub Pages** | 仅前端静态页（`npm run build` 后的 `dist/`） | Java 后端、MySQL、上传文件、直播流 |
| **GitHub 仓库** | 源码托管、CI 构建 | 不能当服务器跑 Spring Boot |

**推荐架构（视频/资源不放本机）：**

```
用户浏览器
    ↓ HTTPS
云服务器（Nginx）
    ├─ /          → 前端静态文件（或对象存储 + CDN）
    ├─ /api       → 反向代理到 Spring Boot :8080
    └─ /uploads   → 对象存储（阿里云 OSS / 腾讯云 COS）或 Nginx 静态目录

云数据库 MySQL（RDS）
对象存储 OSS/COS：视频、封面、头像（后端 upload 改为直传 OSS）
可选：CDN 加速点播；直播用云直播或自建 SRS
```

**简要步骤：**

1. 购买云服务器（2核4G 起步）+ 云数据库 MySQL
2. 服务器安装 JDK 25、Nginx；`mvnw package` 部署 jar，`systemd` 守护进程
3. 创建 OSS/COS 桶，后端 `upload.path` 改为 OSS SDK 上传（或先用服务器磁盘 + 定期同步 OSS）
4. `npm run build` 前端，`dist` 放到 Nginx 或 OSS 静态网站
5. 域名备案后配置 HTTPS（Let's Encrypt 或云证书）
6. `application.yml` / 环境变量配置数据库、OSS、JWT 密钥（勿提交仓库）

### 数据库增量（v0.6，已有库请执行）

```sql
ALTER TABLE live_rooms ADD COLUMN session_start DATETIME AFTER is_live;
ALTER TABLE live_rooms ADD COLUMN ended_at DATETIME AFTER session_start;
ALTER TABLE live_rooms ADD COLUMN create_time DATETIME DEFAULT CURRENT_TIMESTAMP AFTER ended_at;
```

---

### v0.5

### 问题：

- [x] 1.视频发布和修改进入审核；管理员审核通过后才公开展示；发布者只能选「他人可见 / 仅自己可见 / 删除稿件」，不能自选发布状态
  - 状态：0 待审核、1 已发布、2 举报待复审、3 仅自己可见
  - 创作中心上传/编辑使用 `visibility=public|private`；删除用「删除稿件」按钮

- [x] 2.管理员前端：待审视频、举报复审、通过/驳回/删除
  - 路由：`/admin/pending`、`/admin/report`（需 `role=2`）
  - 已发布视频被举报 **3 次** 自动进入复审（`status=2`）

- [ ] 3.融入 AI 审核（**未实现**，方案见下）

- [x] 4.直播内容完善：创作中心「我的直播」— 创建/开播/停播/进入直播间

- [ ] 5.内置 AI 以图搜视频（**未实现**，方案见下）

### AI 审核实现思路（供参考）

1. **触发点**：视频进入 `status=0` 待审时，异步投递审核任务（消息队列或 `@Async`）。
2. **内容抽取**：封面 OCR、视频关键帧（如每 30s 一帧）、标题/简介文本。
3. **模型**：调用云厂商内容安全 API（阿里云/腾讯云）或自建多模态模型，输出标签与置信度。
4. **策略**：
   - 高风险 → 自动驳回（`status=3`）并通知作者；
   - 中风险 → 保留待审，管理员优先处理；
   - 低风险 → 可配置自动通过（`status=1`）或仍人工终审。
5. **表扩展**：`video_audit_logs(video_id, source, result, detail, create_time)` 留痕。

### AI 以图搜视频实现思路（供参考）

1. **入库**：上传/转码完成后，对关键帧提取视觉向量（CLIP / 国产多模态 embedding），写入向量库（Milvus、pgvector、Elasticsearch dense_vector）。
2. **查询**：用户上传图片 → 同模型提取 query 向量 → ANN 近邻检索 → 聚合到 `video_id`（按帧命中次数排序）。
3. **可选**：结合 ASR 字幕文本检索，支持「用语言描述找片段」。
4. **接口**：`POST /search/by-image`（multipart 图片 + topK）。

### 数据库增量（v0.5，已有库请执行）

```sql
ALTER TABLE videos ADD COLUMN report_count INT DEFAULT 0 AFTER status;

CREATE TABLE IF NOT EXISTS video_reports (
  id INT PRIMARY KEY AUTO_INCREMENT,
  video_id INT NOT NULL,
  reporter_id INT NOT NULL,
  reason VARCHAR(500),
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (video_id) REFERENCES videos(id),
  FOREIGN KEY (reporter_id) REFERENCES users(id),
  UNIQUE KEY uk_video_reporter (video_id, reporter_id)
);
```

### 新增接口摘要（v0.5）

| 接口 | 说明 |
|------|------|
| `POST /video/upload` | 增加 `visibility=public\|private` |
| `POST /video/update` | 增加 `visibility`；他人可见时重新待审 |
| `POST /video/visibility` | 发布者切换可见范围 |
| `POST /video/report` | 举报视频（3 次进复审） |
| `GET /admin/video/pending` | 待审列表 |
| `GET /admin/video/report-review` | 举报复审列表 |
| `POST /admin/video/approve` | 审核通过 |
| `POST /admin/video/reject` | 驳回为仅自己可见 |
| `POST /admin/video/delete` | 管理员删除 |
| `GET /live/my/list` | 我的直播间 |

---

### v0.4

### 问题：

- [x] 1.希望支持单设备多用户登录
  - 导航头像菜单：切换账号 / 添加账号 / 退出当前 / 退出全部

- [x] 2.视频界面作者要有关注按钮

- [x] 3.视频编辑功能未完善，完善视频修改
  - 可改标题、简介、封面、视频文件、发布状态

- [x] 4.上传和编辑统合进「发布」→ `/studio`（侧栏：上传视频 / 修改视频）

### v0.3

### 问题：

- [x] 1.没有取消订阅功能，将订阅改为关注，未关注时按钮显示关注，已关注则显示为白底蓝字底带蓝色边框的已关注，评论旁边不应有关注按钮，而在个人展示主页应该有关注按钮。

- [x] 2.播放历史应当展示视频封面


### v0.2

### 问题：

- [x] 1.增加通知栏，他人的点赞应有提示，增加私信功能，用户可以给别的用户发消息，UI上私信消息提示包括在通知里显示对应的对话room点击进入，按照时间顺序从新到旧排列。进入他人的个人展示页后应当有一个发信息的图标，点击后进入room，room内是点击的用户和展示的用户，这里用图片，给出路径稍后绘制补充。
  - 图标占位：`/messages/bell.png`（通知）、`/messages/send.png`（发私信）
  - 路由：`/messages/:roomId`

- [x] 2.好多弹窗提示的信息变成“？？？？”了，包括很多比如用户名显示也有多出奇怪的“？”

- [x] 3. 喜欢和不喜欢的图标放在了public/like文件夹下，网站icon更改了，你顺便把首页和什么地方的网站图标改成这个favicon.ico
  - 赞/踩：`/like/like.png`、`/like/dislike.png`
  - 站点图标：`/favicon.ico`（`index.html` 与导航 logo）

### 数据库增量（v0.2，已有库请执行）

```sql
CREATE TABLE IF NOT EXISTS notifications (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL,
  type INT NOT NULL,
  actor_id INT NOT NULL,
  ref_id INT,
  preview VARCHAR(255),
  is_read BOOLEAN DEFAULT FALSE,
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id),
  FOREIGN KEY (actor_id) REFERENCES users(id)
);

CREATE TABLE IF NOT EXISTS dm_rooms (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_a INT NOT NULL,
  user_b INT NOT NULL,
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_users (user_a, user_b),
  FOREIGN KEY (user_a) REFERENCES users(id),
  FOREIGN KEY (user_b) REFERENCES users(id)
);

CREATE TABLE IF NOT EXISTS dm_messages (
  id INT PRIMARY KEY AUTO_INCREMENT,
  room_id INT NOT NULL,
  sender_id INT NOT NULL,
  content TEXT NOT NULL,
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (room_id) REFERENCES dm_rooms(id),
  FOREIGN KEY (sender_id) REFERENCES users(id)
);
```

### 新增接口摘要（v0.2）

| 接口 | 说明 |
|------|------|
| `GET /notification/list` | 通知列表（新→旧） |
| `GET /notification/unread-count` | 未读数 |
| `POST /notification/read` | 标已读（可选 id，不传则全部） |
| `POST /message/room/open` | 打开/创建私信 room |
| `GET /message/room/get` | 拉取 room 消息 |
| `POST /message/send` | 发送私信 |
---
### v0.1

### 问题：

- [x] 1. 头像改为本地上传：个人中心点击头像 → 确认 → 从电脑选图（不再填 URL）
- [x] 2. 评论支持点赞、点踩、复制；**系统 Unicode + 图片表情**（图片放 `web/public/emojis/`，配置见 `src/constants/emojis.js`）
- [x] 3. 点击他人头像进入个人展示页（简介 + 已发布作品，不展示账号等隐私）
- [x] 4. 视频支持点赞、点踩、转发（复制链接）
- [x] 5. 个人中心可编辑昵称、个人简介，并可预览公开主页

### 数据库增量（已有库请执行）

```sql
ALTER TABLE users ADD COLUMN bio TEXT AFTER avatar;

CREATE TABLE IF NOT EXISTS video_reactions (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL,
  video_id INT NOT NULL,
  reaction TINYINT NOT NULL,
  UNIQUE KEY uk_user_video (user_id, video_id),
  FOREIGN KEY (user_id) REFERENCES users(id),
  FOREIGN KEY (video_id) REFERENCES videos(id)
);

CREATE TABLE IF NOT EXISTS comment_reactions (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL,
  comment_id INT NOT NULL,
  reaction TINYINT NOT NULL,
  UNIQUE KEY uk_user_comment (user_id, comment_id),
  FOREIGN KEY (user_id) REFERENCES users(id),
  FOREIGN KEY (comment_id) REFERENCES comments(id)
);
```

### 新增接口摘要

| 接口 | 说明 |
|------|------|
| `POST /user/avatar/upload` | 上传头像 |
| `POST /user/info/update` | 更新昵称、简介（`bio`） |
| `GET /user/profile/showcase` | 公开展示页 |
| `POST /video/reaction` | 视频赞/踩 |
| `POST /comment/reaction` | 评论赞/踩 |
