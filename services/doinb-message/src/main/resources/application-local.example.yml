spring:
  datasource:
    url: jdbc:mysql://localhost:3306/doinb_msg?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: "改成你的 MySQL 密码"

doinb:
  services:
    user: http://127.0.0.1:8082

jwt:
  secret: "改成至少 32 个字符的随机字符串用于 JWT 签名"
