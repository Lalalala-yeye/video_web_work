# 项目和架构说明

## 1. 项目目标与业务完成情况

**目标**：做一套课设规模的「doinb 视频播放与内容服务平台」，把观看、创作、互动、审核放在同一套 Web 里，解决功能分散、发布和观看割裂的问题。

**1.0 范围（已完成）**：访客注册登录；用户浏览播放、历史续播、评论赞踩、关注订阅、搜索、私信通知、进直播间；作者上传编辑视频、开关播；管理员审核与举报复审。共 **15 个用例、10 个功能域（M1～M10）、44 个对外 API（OP-001～OP-044）**。

| 用例 | 名称 | 状态 |
|---|---|---|
| UC-01 | 用户注册 | 完成 |
| UC-02 | 用户登录/登出 | 完成 |
| UC-03 | 个人信息维护 | 完成 |
| UC-04 | 浏览与播放视频 | 完成 |
| UC-05 | 播放历史 | 完成 |
| UC-06 | 上传与编辑视频 | 完成 |
| UC-07 | 举报视频 | 完成 |
| UC-08 | 直播管理 | 完成 |
| UC-09 | 关注与订阅动态 | 完成 |
| UC-10 | 发表评论 | 完成 |
| UC-11 | 赞踩互动 | 完成 |
| UC-12 | 关键词搜索 | 完成 |
| UC-13 | 通知 | 完成 |
| UC-14 | 私信 | 完成 |
| UC-15 | 管理员审核 | 完成 |

**完成情况**：需求—设计—代码—测试按同一套 UC 编号对齐。《测试报告》记录系统测试 **59/59 通过**，对象级单测 **179**、MockMvc **80**（合计 259，失败 0），Selenium 验收覆盖 15 个用例主路径。前端仍是一份 Vue，后端已从单体拆成 **网关 + 5 个业务服务**，本机 K8s 可部署、可滚动更新。SRS 2.0 的 AI 审核、以图搜视频未做。

---

## 2. 代表性用例：需求 → 设计 → 代码 → 测试

不按 15 个用例拆 15 个服务。答辩讲 3 条最能说明追溯和跨服务协作的链。完整矩阵见 `文档-已确认/需求追溯矩阵.md`。

### 用例 A：UC-01 用户注册（单服务闭环）

| 层次 | 落点 |
|---|---|
| 需求 | SRS 3.4.1：用户名唯一、两次密码一致、BCrypt 存哈希 |
| 概要设计 | 图 2-1 组件顺序图：`RegisterView` → `UserAccountController` → `UserAccountService` → `UserMapper` |
| 详细设计 | M1 顺序图 2-2 |
| 代码 | 用户服务 `UserAccountController.register` / `UserAccountServiceImpl`，写 `users` |
| 测试 | U000 成功、U001 密码不一致、U002 账号已存在；E2E `01-auth.js` |

### 用例 B：UC-10 发表评论（跨服务校验）

| 层次 | 落点 |
|---|---|
| 需求 | 视频评论 `targetType=1`；直播弹幕 `targetType=2`，未开播拒绝 |
| 设计 | 评论在互动服务；目标是否存在不查别人的表，走内部 HTTP |
| 代码 | `CommentServiceImpl`：视频调 `/internal/videos/{id}`，直播调 `/internal/lives/{id}`；不存在 404，未开播 400 |
| 测试 | C000/C001/C002/C010；直播 L040（停播禁弹幕）、L041（开播可弹幕） |

### 用例 C：UC-12 搜索 + UC-15 审核（网关聚合 / 跨服务通知）

- **搜索**：需求是一次返回视频、用户、直播。拆分后搜索不独立成服务，由网关并行打三个 `/internal/search/*`，对外路径仍是 `GET /search`。测试 S000～S002。
- **审核**：管理员通过后视频服务改 `videos.status`，再 `POST` 消息服务 `/internal/notifications`（type=4）。测试 A000/A010；通知侧 N000 能看到「已通过审核」。

其余用例按同一矩阵对齐，例如 UC-06 上传 → `VideoController.upload` → V030；UC-08 直播 → `LiveRoomController` + SRS → L020～L022。

---

## 3. 模型到代码

领域概念类图（SRS 图 3-2）里的实体，一一落到 Java 实体、Mapper 和表，不在服务间做跨库 JOIN。

| 领域实体 | 代码类 | 表 | 归属服务 |
|---|---|---|---|
| User | `doinb-user` 的 `User` | `users` | 用户 |
| Video | `Video` | `videos` | 视频 |
| PlayHistory | `PlayHistory` | `play_history` | 视频 |
| VideoReport | `VideoReport` | `video_reports` | 视频 |
| LiveRoom | `LiveRoom` | `live_rooms` | 直播 |
| Comment / Reaction / Subscription | `Comment`、`VideoReaction`、`CommentReaction`、`Subscription` | `comments` 等 4 张 | 互动 |
| Notification / DmRoom / DmMessage | 同名实体 | `notifications`、`dm_rooms`、`dm_messages` | 消息 |

调用链固定为：

**Vue 页面 → `web/src/api/*.js` → 网关 8081 → 对应 Controller → Service → Mapper → 本服务表**

网关验 JWT 后注入 `X-User-Id` / `X-User-Role`，下游不再解析 JWT、也不回问用户服务「这人登录了没」。其它服务需要昵称、视频是否存在时，只保存对方业务 ID，通过内部接口取 DTO。

---

## 4. 微服务怎么拆

**划分依据**：按功能域 + 数据归属拆，不按页面、也不按用例个数拆。前端不拆；搜索没有独立库，放在网关聚合。媒体面仍归 SRS，Java 只管房间元数据。课设期可共用一台 MySQL，逻辑上分 schema，禁止跨服务 JOIN。

| 服务 | 端口 | 职责（用例） | 只写的表 | 对外路径前缀 |
|---|---|---|---|---|
| 用户 `doinb-user` | 8082 | UC-01～03 注册登录资料 | `users`；头像文件 | `/user`、`/admin/account` |
| 视频 `doinb-video` | 8083 | UC-04～07、UC-15 点播/上传/举报/审核 | `videos`、`play_history`、`video_reports`；成片与封面 | `/video`（不含 reaction）、`/admin/video` |
| 直播 `doinb-live` | 8084 | UC-08 房间与开停播 | `live_rooms` | `/live` |
| 互动 `doinb-interact` | 8085 | UC-09～11 评论、赞踩、关注 | `comments`、`*_reactions`、`subscriptions` | `/comment`、`/subscription`、`/video/reaction` |
| 消息 `doinb-message` | 8086 | UC-13～14 通知、私信 | `notifications`、`dm_*` | `/notification`、`/message` |
| 网关 `doinb-gateway` | 8081 | 鉴权转发；UC-12 搜索聚合 | 无业务表 | `/search`、`/health` |

### 服务间调用

同步 HTTP + `X-Internal-Token`，浏览器打 `/internal/**` 一律 403。

- 审核通过：视频 → 消息 `POST /internal/notifications`
- 点赞：互动 → 同上（type=1/2）
- 发评论：互动 → 视频或直播 `GET /internal/.../{id}`
- 关注：互动 → 用户 `GET /internal/users/{id}`
- 订阅动态：互动 → 视频 `by-authors` + 直播 `by-anchors`
- 搜索：网关并行 → 用户/视频/直播三个 `/internal/search/*`
- 拼昵称：各服务 → 用户 `GET /internal/users?ids=`

互动服务本期不对他人提供内部接口。私信通知在消息服务内部写，不再 HTTP 调自己。

### 数据归谁管理

其它服务需要数据时走接口，不跨库 JOIN。服务内部可以保留外键；跨服务不使用数据库外键，只保存对方业务 ID。

| 表 | 归属服务 | 其它服务如何使用 |
|---|---|---|
| `users` | 用户 | 只保存 `user_id` / `author_id` / `anchor_id` |
| `videos` | 视频 | 互动/搜索通过接口读标题、状态、封面 |
| `play_history` | 视频 | 仅视频服务写入播放进度 |
| `video_reports` | 视频 | 举报与复审只在视频服务内完成 |
| `live_rooms` | 直播 | 表内存 `stream_key`、`is_live`；媒体面归 SRS |
| `comments` | 互动 | `target_type`：1=视频，2=直播间 |
| `comment_reactions` | 互动 | 只关联本服务评论 |
| `video_reactions` | 互动 | 视频 ID 为外部引用 |
| `subscriptions` | 互动 | 关注关系只由互动服务维护 |
| `notifications` | 消息 | 其它服务只调用「创建通知」接口 |
| `dm_rooms` / `dm_messages` | 消息 | 仅消息服务读写 |

文件：头像归用户服务；视频文件与封面归视频服务。

---

## 5. 调用失败时怎么处理

按「主路径不能被旁路拖死」来分层。

1. **同步查询失败（评论、点赞、关注）**  
   `ServiceClient` 读超时 8 秒，捕获后返回 `code=502`。互动侧把「下游非 200」当成目标不存在：评论/点赞返回 404「视频不存在」，不写脏数据。

2. **旁路通知失败**  
   审核通过、点赞后写通知是尽力而为。`MessageNotifier` 吞掉异常：**消息服务挂了，审核结果和点赞结果不回滚**。

3. **搜索降级（可演示）**  
   网关三路并行，单路 **3 秒超时**。某一路失败：整体仍 `code=200`，该分类列表为空，`notices` 里写「视频服务超时或不可用…」。用户、直播不受影响；网关 `/health` 仍正常。脚本：`deploy/k8s/fault-demo.ps1`（停掉 video 后再搜一次）。

4. **网关转发**  
   普通请求 30 秒超时，上传 10 分钟；未知路径 404；`/internal` 对浏览器 403。

5. **进程/集群**  
   各服务有 `/health` `/ready` `/version`；K8s 探针失败则摘流量。滚动更新 `maxUnavailable: 0`，新 Pod Ready 后再杀旧 Pod；救不回来可 `rollout undo`。

---

## 6. 测试、CI/CD、扩缩容、性能对比

### 单元测试 / 集成测试 / 端到端测试

| 层次 | 口径 | 条数 | 结果 |
|---|---|---|---|
| 单元（对象级 Service） | 单体 `*ServiceImplTest` | 179 | 179 通过 |
| API 契约（MockMvc，不启 MySQL） | 随 `mvn test` | 80 | 80 通过 |
| 微服务单测（CI `services-test`） | 五服务 + 网关 JUnit | 约 171 条 `@Test` | 门禁，失败则不打镜像 |
| 系统/集成 | Postman 打真实后端（网关 + 五服务 + MySQL） | 报告 59 条；CI 用全量集合 | 59/59 通过 |
| 端到端 | Selenium `web/e2e/01`～`05`，覆盖 15 个 UC | 报告 54 个 GUI 检查点 | 全过 |

课设原要求不单独做「组件级集成套件」。拆服务后，Newman 打网关串联五服务，承担跨进程集成验证。CI 里 E2E 是 `continue-on-error`，失败出截图、不挡住打镜像。

系统测试按模块：健康 1、用户 13、视频 12、评论 4、赞踩 3、关注 4、搜索 3、直播 7、通知 3、私信 3、审核 6，合计 59。

### CI/CD 流水线

Push 后 GitHub Actions 顺序：

1. **CI 单测**：`services/` 五服务 + 网关 JUnit（不启 MySQL）
2. **CI 集成**：起 MySQL → 建表 + seed → 起五服务 + 网关 8081 → Newman 全量公开接口
3. **CI 验收**：Selenium E2E（失败不挡门禁）
4. **CD 打镜像**：7 个 SHA tag 推 GHCR（gateway / user / video / live / interact / message / web），禁用 `latest`
5. **CD 部署探活**：kind 集群部署本次 SHA，探活 `/health` `/ready` `/version`

本机答辩环境（`scripts/`）：

- `k8s-up.ps1`：首次部署到本机 Kubernetes（namespace `doinb`）
- `local-cd.ps1 -Watch`：等 CI 出镜像后滚动更新本机 Deployment

浏览器 http://localhost:8787，左上角版本号变成新 SHA，即一次完整 CD。

### 自动扩缩容

HPA 只挂 **gateway / video / user**（压测会打到的路径），副本 **1～4**，CPU 目标 **50%**（相对 request）。扩容立即最多翻倍；缩容先稳 **120 秒**，再每分钟最多减 20%。MySQL、live、interact、message、web 不挂 HPA。

演示：`deploy/k8s/hpa-demo.ps1`，登录 50 VU、约 90 秒，现场看 `kubectl get hpa -w` 副本增加。

### 故障处理

主动停视频服务后：搜索仍 200、`videos=[]`、带 notices；用户/直播结果仍在；网关健康检查正常。这是隔离，不是整站挂掉。脚本：`deploy/k8s/fault-demo.ps1`。

### 单体 vs 微服务性能

同一台机、同一脚本、50 VU / 30s / 3 轮，HPA 关闭。记录见 `bench/TEMPLATE.md`（2026-09-02），两边错误率均为 **0**：

| 接口 | 单体吞吐 | 微服务吞吐 | 单体均时 | 微服务均时 | 内存合计 |
|---|---:|---:|---:|---:|---:|
| 登录 | 32.4 req/s | 33.0 req/s | 1.55 s | 1.54 s | 0.85 / 1.88 GiB |
| 视频列表 | **645** req/s | **116** req/s | 118 ms | 443 ms | 1.05 / 2.00 GiB |
| 搜索 | **474** req/s | **83** req/s | 162 ms | 650 ms | 1.05 / 2.06 GiB |

**怎么讲**：登录被 BCrypt 卡住，拆不拆几乎一样。列表和搜索单体明显更快，因为微服务多一跳网关、六个 JVM，单机对比会吃亏；内存大约翻倍。换来的是按表拆库、按服务扩容、搜索一路挂了还能降级。课设要的是「能对比、能解释」，不是微服务在单机上更快。

---

## 7. 口头收束

需求 15 个用例全部按 RTM 落到代码和测试；服务按「谁拥有哪张表」拆成 5 个业务进程加网关；质量上单测和 59 条系统测试全过，运行上有 CI/CD、HPA 和搜索降级，性能上单机读路径单体更好、登录持平，这是预期代价。
